#include "camera1.h"


camera::camera(int mode) {
    if (mode == 0) {
        // wiringPiSetupPhys();
    } else {

    }
}

int set_camera(int cam_num) {
    return 0;
}

std::string capture(std::string name) {
    return name;
}
